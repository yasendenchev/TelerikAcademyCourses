function solve(args) {
    let len = args[0];
    let arr = args[1].split(' ');
    let x = args[2];
    function countAppearanceOf(x, array) {

        for (let i = 0; i < len; i++) {

        }
    }
}